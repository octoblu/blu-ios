# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Blu' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 3.4'
  pod 'SwiftyJSON', '~> 2.3'
  pod 'SwiftyOAuth', '~> 0.3'

  # Pods for Blu

  target 'BluTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'BluWatch' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BluWatch

end

target 'BluWatch Extension' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BluWatch Extension

end
